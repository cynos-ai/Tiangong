# Local configuration and generated runtime state
.env
.env.*
!.env.example
.runtime/
node_modules/
__pycache__/
*.py[cod]
npm-debug.log*
# Probe build output (regenerated by the fixture Dockerfile)
smoke-testing/fixtures/p0-identity-pg/app/dist/

# Local research/build scratch and generated smoke output
/.tmp-codex-extension/
/.tmp-codex-package/
/.tmp-openclaw-codex-source/
/output/
/tmp/
# Gate B and nested Leader smoke runs write credential-free diagnostics and
# normalized helper copies beside the source scripts. They are disposable
# run state, not repository fixtures or evidence.
/.tmp-gateb-*/
/.tmp-*.out
/.tmp-*.err
/.tmp-runner-broker/
/smoke-testing/support/.run-leader-smoke-*.sh

# Local private working materials (keep out of the public tree)
/artifacts/
/.cynos/
