# Tiangong Workspace Skill v1

Use only the fixed workspace tools exposed by the runtime. Keep filesystem
access inside the authenticated workspace and treat machine Evidence and
approval state as authoritative.
